begin library "threads"
  generated = true
  version = "[distributed with Ocaml]"
  dirname = [ "%{OCAMLLIB}%/threads" ]
  requires = [ "unix" ]
end

begin config "threads"
  asmcomp += [ "-thread" ]
  asmlink += [ "-thread" ]
end
